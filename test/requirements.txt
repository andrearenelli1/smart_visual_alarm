ai-edge-litert>=1.0        # TFLite runtime (replaces tf.lite.Interpreter, handles old models)
tensorflow-datasets>=4.9   # VWW / wake_vision dataset download
importlib_resources>=6.0   # required by wake_vision TFDS builder
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
Pillow>=10.0
